import React from 'react';
import {Text, View} from 'react-native';

interface authProps {}

const SignInScreen = (props: authProps) => {
  return (
    <View>
      <Text>SignInScreen</Text>
    </View>
  );
};

export default SignInScreen;
